NEXT_PUBLIC_SITE_URL=http://localhost:3000

# E-Mail-Versand für /api/checklist-submit (src/lib/server/email/).
# EMAIL_PROVIDER unset oder "console" -> loggt statt zu versenden (sicherer
# Default für lokale Entwicklung, keine Zugangsdaten nötig).
EMAIL_PROVIDER=console
# EMAIL_PROVIDER=resend
# RESEND_API_KEY=
# EMAIL_FROM=
